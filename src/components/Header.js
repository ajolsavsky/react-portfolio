import React from 'react';

function Header() {
    const header = 'I am the header!';

    return (
        <div className="container">
            <h2>{header}</h2>
        </div>
    );
}

export default Header;